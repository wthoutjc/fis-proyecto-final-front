import { createTheme } from "@mui/material";
import {} from "@mui/material/colors";

// Colors

const darkTheme = createTheme({
  palette: {
    mode: "dark",
    background: {
      default: "#000",
    },
  },
});

export { darkTheme };
